// import React, { useState } from 'react';

// import type { FullExcursion} from '../types/api';

// const [currentExcursion, setCurrentExcursion] = useState<Partial<FullExcursion> | null>(null);

// // В JSX родителя:
// if (isEditing) {
//   return (
//     <ExcursionEditor 
//       data={currentExcursion} 
//       onChange={setCurrentExcursion} 
//       onBack={handleBack} 
//     />
//   );
// }